name: Module 3 - Adding a Disk
version: 1.0.0
formatVersion: 1
inputs: {}
resources:
  ubuntu:
    type: Cloud.vSphere.Machine
    properties:
      image: ubuntu
      flavor: small
      networks:
        - network: ${resource.network.id}
          assignment: static
  network:
    type: Cloud.NSX.Network
    properties:
      networkType: existing

